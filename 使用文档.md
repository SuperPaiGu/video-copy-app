# 视频文案生成器 - 使用指南

这个应用可以自动为您的视频生成抖音文案，操作非常简单。

---

## 📦 第一次使用前的准备

### 步骤1：安装Node.js（必须）

1. 打开浏览器，访问：https://nodejs.org/
2. 点击左边绿色的"LTS"版本下载
3. 下载完成后，双击安装包
4. 安装过程中一直点"下一步"即可（保持默认设置）
5. 安装完成后，**重启电脑**

**为什么需要Node.js？**
这是应用运行必需的基础软件，就像手机需要安装微信才能聊天一样。

### 步骤2：安装Docker Desktop（必须）

1. 访问：https://www.docker.com/products/docker-desktop/
2. 点击"Download for Windows"下载
3. 下载完成后，双击安装包安装
4. 安装过程中一直点"下一步"即可
5. 安装完成后，双击桌面上的"Docker Desktop"图标启动
6. 等待右下角Docker图标变为绿色（表示运行正常）

**为什么需要Docker？**
这是应用的"数据仓库"，用来临时存储视频处理信息。

**重要提示**：
- Docker Desktop需要一直运行，不要关闭它
- 如果不小心关闭了，重新打开即可
- Docker图标在右下角系统托盘（时钟旁边）

---

## 🚀 启动应用（每次使用前必做）

### 简单3步启动

1. **确保Docker正在运行**
   - 看右下角系统托盘（时钟旁边）
   - 找到Docker图标（鲸鱼图案）
   - 如果图标是绿色 ✅ 说明正在运行
   - 如果没有图标，双击桌面"Docker Desktop"启动

2. **进入应用文件夹**
   - 打开"此电脑"
   - 找到文件夹：`视频文案生成应用`

3. **双击启动**
   - 找到文件：`一键启动.bat`
   - 双击运行
   - 会弹出黑色窗口，等待约10-20秒
   - 看到"启动成功"字样后，打开浏览器
   - 访问：http://localhost:3000

**重要提示**：
- 黑色窗口**不要关闭**，关闭后应用会停止
- 可以最小化黑色窗口，但不要点右上角的"×"
- 如果不小心关闭了，重新双击"一键启动.bat"即可

---

## 📹 使用应用

### 上传视频

1. 在浏览器中看到上传区域（虚线框）
2. 两种上传方式：
   - **拖拽上传**：把视频文件直接拖到虚线框里
   - **点击上传**：点击虚线框，选择视频文件

### 支持的视频格式

- ✅ MP4格式（最常见）
- ✅ MOV格式（iPhone录制）
- ✅ WEBM格式
- ⚠️ 单个视频最大：1GB
- ⚠️ 一次最多上传：10个视频

### 查看任务进度

上传后，页面会显示任务卡片：

**当前任务**（正在处理的视频）：
- 🟡 **排队中** - 等待处理
- 🔵 **处理中** - 正在生成文案（大约需要6-9秒）
- ✅ **已完成** - 文案生成成功
- ❌ **失败** - 处理出错

**历史记录**（之前上传的视频）：
- 点击任务卡片可以展开查看文案
- 再次点击标题栏可以折叠
- 点击"删除批次"可以删除整批历史记录

### 查看和复制文案

处理完成后，任务卡片会显示3个文案变体：

**每个变体包含**：
- **标题**：吸引眼球的短标题
- **正文**：完整的视频文案
- **话题标签**：相关的抖音话题（带#号）

**复制文案**：
1. 在"当前任务"或"历史记录"中找到视频
2. 如果是历史记录，点击展开
3. 用鼠标选中想要的文案
4. 按 `Ctrl+C` 复制
5. 粘贴到抖音或其他地方使用

---

## 🛑 关闭应用（使用完毕后必做）

### 正确关闭步骤

1. **进入应用文件夹**
   - 打开"此电脑"
   - 找到文件夹：`视频文案生成应用`

2. **双击停止脚本**
   - 找到文件：`停止服务.bat`
   - 双击运行
   - 等待提示"所有服务已停止"

3. **手动关闭所有黑色窗口**
   - 找到所有黑色的命令行窗口（CMD窗口）
   - 点击每个窗口右上角的"×"关闭
   - 通常有1个窗口需要关闭（总之关完就是了）

**重要提示**：
- 停止脚本只会停止应用服务，**不会自动关闭窗口**
- 必须手动关闭所有黑色窗口
- 如果不关闭窗口，下次启动可能会提示"端口被占用"

**为什么要正确关闭？**
- 如果不正确关闭，下次启动可能会提示"端口被占用"
- 正确关闭可以释放电脑资源，让电脑运行更流畅

**安全提示**：
- 停止脚本只会关闭本应用的进程
- 不会影响电脑上其他正在运行的程序
- 可以放心使用

---

## 🔧 常见问题

### 问题1：双击"一键启动.bat"后提示"未安装Node.js"

**解决方法**：
1. 按照"步骤1：安装Node.js"操作
2. 安装完成后**必须重启电脑**
3. 重启后再次双击"一键启动.bat"

### 问题2：提示"Docker未运行"

**解决方法**：
1. 双击桌面上的"Docker Desktop"图标
2. 等待右下角Docker图标变为绿色
3. 再次双击"一键启动.bat"

### 问题3：提示"端口3000已被占用"

**原因**：上次使用后没有正确关闭应用

**解决方法**：
1. 双击 `停止服务.bat`
2. 等待10秒
3. 再次双击"一键启动.bat"

### 问题4：黑色窗口一闪而过

**原因**：可能是Node.js或Docker没有正确安装

**解决方法**：
1. 确认已安装Node.js（重启电脑后）
2. 确认Docker Desktop正在运行（右下角绿色图标）
3. 如果还是不行，重启电脑后再试

### 问题5：视频上传后一直显示"处理中"

**可能原因**：
- 视频文件过大（超过1GB）
- 视频时长过长（超过5分钟）
- 网络连接问题

**解决方法**：
1. 等待5-10分钟（大视频需要更长时间）
2. 检查网络连接是否正常
3. 如果超过10分钟还在处理中，双击"停止服务.bat"，然后重新启动

### 问题6：生成的文案质量不满意

**建议**：
- 确保视频内容清晰
- 确保视频有清晰的语音
- 可以多次上传同一视频，选择最满意的文案

---

## 💡 使用技巧

### 技巧1：批量处理

可以一次上传多个视频（最多10个），系统会自动排队处理，不需要等一个处理完再上传下一个。

### 技巧2：历史记录管理

- **当前任务**：显示本次上传的视频，完成后会留在这里
- **历史记录**：下次上传新视频时，之前的视频会自动转移到历史记录
- 历史记录按批次分组，可以一键删除整批记录

### 技巧3：文案选择

每个视频会生成3个不同风格的文案，您可以：
- 直接使用其中一个
- 混合使用多个文案的优点
- 根据需要修改文案内容

---

## 📞 遇到问题怎么办

如果遇到无法解决的问题：

### 方法1：重启应用
1. 双击 `停止服务.bat`
2. 等待10秒
3. 双击 `一键启动.bat`

### 方法2：重启Docker
1. 右键点击右下角Docker图标
2. 选择"Quit Docker Desktop"
3. 重新双击桌面"Docker Desktop"图标启动
4. 等待图标变绿后，双击"一键启动.bat"

### 方法3：重启电脑
如果以上方法都不行，重启电脑通常可以解决大部分问题。

---

## 🎉 快速回顾

### 第一次使用（只需做一次）

1. ✅ 安装Node.js → **重启电脑**
2. ✅ 安装Docker Desktop → 启动Docker
3. ✅ 双击"一键启动.bat"
4. ✅ 打开浏览器访问 http://localhost:3000

### 日常使用（每次使用）

**启动应用**：
1. ✅ 确认Docker正在运行（右下角绿色图标）
2. ✅ 双击"一键启动.bat"
3. ✅ 等待黑色窗口显示"启动成功"
4. ✅ 打开浏览器访问 http://localhost:3000

**使用应用**：
1. ✅ 上传视频
2. ✅ 等待文案生成
3. ✅ 复制文案使用

**关闭应用**：
1. ✅ 双击"停止服务.bat"
2. ✅ 等待提示"所有服务已停止"
3. ✅ 手动关闭所有黑色窗口（点击右上角"×"）

---

## ⚠️ 重要提醒

1. **黑色窗口不要关闭**
   - 应用运行时会有黑色窗口
   - 可以最小化，但不要点"×"关闭
   - 关闭后应用会停止

2. **Docker要一直运行**
   - 右下角Docker图标要保持绿色
   - 如果变成灰色，重新打开Docker Desktop

3. **使用完要正确关闭**
   - 双击"停止服务.bat"
   - 等待提示"所有服务已停止"
   - 手动关闭所有黑色窗口（通常2-3个）

4. **遇到问题先重启**
   - 大部分问题重启应用可以解决
   - 实在不行就重启电脑

---

祝您使用愉快！🎬

如有问题，请联系技术支持。
